version: 2

sources:
  - name: flight_db
    schema: flight_db          # namespace inside Polaris  
    config:
      meta:
        technical_owner: PeterMuster
        data_tier: Raw
    tables:
      - name: airport_raw_t

      - name: flight_raw_t
